{
  "functions": {
    "api/index.js": {
      "includeFiles": "{*.{html,txt,xml},_headers,favicon*,c*/**,script/**,libs/**,images/**,legal/**,owner/**,pages/**,plugin*/**,recursos/**,store-builder/**,upload/**,willie*/**,ayuda/**,server/**,music*/**,videos/**}",
      "excludeFiles": "{**/*.{wav,docx,psd,map,pkg},videos/video_para_modal.mp4}",
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
